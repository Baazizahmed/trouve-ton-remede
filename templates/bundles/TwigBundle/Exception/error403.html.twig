{% extends "themes/base_visitor.html.twig" %}

{% block title %}Accès Interdit (Erreur 403){% endblock %}
{% block description %}Vous n'avez pas les autorisations nécessaires pour consulter cette page du blog Oumi.{% endblock %}

{# On dit à Google de ne pas indexer cette page #}
{% block meta_robots %}
    <meta name="robots" content="noindex, follow">
{% endblock %}


{% block main %}
<div class="container my-5">
    <div class="row align-items-center justify-content-center">
        
        <div class="col-md-5 d-none d-md-block">
            <img src="/images/static/errors/403-illustration.png" class="img-fluid" alt="Illustration d'un chemin privé">
        </div>

        <div class="col-md-6 text-center text-md-start">
            
            {# La couleur 'danger' de Bootstrap (rouge) est appropriée pour une interdiction #}
            <h1 class="display-1 fw-bold text-danger">403</h1>
            <h2 class="h3 mb-3">Accès réservé</h2>
            <p class="lead text-muted mb-4">
                Ce petit coin du jardin est privé. Seuls les utilisateurs avec la bonne permission peuvent y entrer.
            </p>
            
            <div class="mt-4">
                {# On affiche un lien différent si l'utilisateur est connecté #}
                {% if app.user %}
                    <a href="{{ path('app_user_home') }}" class="btn btn-primary shadow-sm">
                        <i class="fas fa-user-circle me-2"></i>
                        Retourner à mon espace
                    </a>
                {% else %}
                    <a href="{{ path('app_visitor_welcome') }}" class="btn btn-primary shadow-sm">
                        <i class="fas fa-home me-2"></i>
                        Retourner à l'accueil
                    </a>
                {% endif %}
            </div>
        </div>

    </div>
</div>
{% endblock %}