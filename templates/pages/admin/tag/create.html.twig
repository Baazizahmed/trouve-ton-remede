{% extends 'themes/base_admin.html.twig' %}

{% block title %}Nouveau tag{% endblock %}

{% block description %}Nouveau tag{% endblock %}

{% block main %}
<div class="container mt-4">

    {% set header_actions %}
        <a href="{{ path('app_admin_tag_index') }}" class="btn btn-outline-secondary">
            Retour à la liste
        </a>
    {% endset %}

    {% include 'components/admin/page_header.html.twig' with {
        badge: 'Administration',
        title: 'Nouveau tag',
        subtitle: 'Ajoutez un nouveau tag pour organiser les contenus du site.',
        actions: header_actions
    } %}

    <div class="row justify-content-center mt-4">
        <div class="col-md-8 col-lg-6">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    <h2 class="h5 mb-4">Informations du tag</h2>

                    {{ form_start(form) }}

                        <div class="mb-4">
                            {{ form_label(form.name, 'Nom', {'label_attr': {'class': 'form-label fw-semibold'}}) }}
                            <div class="text-danger small mb-1">{{ form_errors(form.name) }}</div>
                            {{ form_widget(form.name, {
                                attr: {
                                    class: 'form-control',
                                    autofocus: 'autofocus',
                                    placeholder: 'Nom du tag'
                                }
                            }) }}
                        </div>

                        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 pt-3 border-top">
                            <p class="text-muted small mb-0">
                                Renseignez un nom clair et facile à réutiliser.
                            </p>

                            <button formnovalidate type="submit" class="btn btn-primary px-4">
                                Créer le tag
                            </button>
                        </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

</div>
{% endblock %}